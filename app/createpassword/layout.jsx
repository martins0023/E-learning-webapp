export const metadata = {
    title: "Login - vconnect",
    description: "Login to access your account",
  };
  
  const CreatepasswordLayout = ({ children }) => {
    return (
      <html lang="en">
        <body>
          <main>{children}</main>
        </body>
      </html>
    );
  };
  
  export default CreatepasswordLayout;
  